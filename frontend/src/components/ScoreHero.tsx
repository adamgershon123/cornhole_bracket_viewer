import type { Match } from '../lib/api'
export function ScoreHero({match}:{match:Match}){const g=match.activeGame||match.games?.[0]; const topScore=g?.score?.top ?? match.score.top ?? 0; const bottomScore=g?.score?.bottom ?? match.score.bottom ?? 0; const lead=topScore===bottomScore?'Tied':topScore>bottomScore?match.teams.top.name:match.teams.bottom.name; return <section className="glass rounded-[28px] p-4 overflow-hidden"><div className="flex items-center justify-between gap-2 text-xs uppercase tracking-widest text-zinc-400"><span>Court {match.courtId||'?'}</span><span>{match.roundDescription}</span><span>{match.status}</span></div><div className="grid grid-cols-[1fr_auto_1fr] gap-3 items-center mt-5"><Team name={match.teams.top.name} score={topScore} align="left"/><div className="text-zinc-600 font-black text-lg">VS</div><Team name={match.teams.bottom.name} score={bottomScore} align="right"/></div><div className="mt-5 rounded-2xl bg-black/35 border border-white/10 p-3 text-center"><div className="text-lg font-black text-amber-300 truncate">{lead}</div></div></section>}
function Team({name,score,align}:{name:string;score:number;align:'left'|'right'}){return <div className={align==='right'?'text-right':'text-left'}><div className="min-h-12 text-xl leading-6 font-black line-clamp-2">{name}</div><div className="text-7xl font-black font-display text-white leading-none mt-2">{score}</div></div>}
